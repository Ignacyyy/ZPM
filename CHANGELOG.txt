ZPM v2.1.9
 - hotfix

ZPM v2.1.8
- Add auto mode for zpm install and zrm remove programs.
- Fix UI bugs
- New progressbar look

ZPM v2.1.7
- UI fixes

ZPM v2.1.6
- hotfix

ZPM v2.1.5
- added terminal UI (zpm tui)

ZPM v2.1.4
- zrm hotfix and log info
- zint hotfix and log info

ZPM v2.1.3
Program updates: (General)
- more info about runing provess
- better stability
- small fixes
- zhome new ui
- updated config

ZPM v2.1.2
-hotfix

ZPM v2.1.1
- hotfix

ZPM v2.1.0
- added support to dnf, zypper, apt
- renamed zhelp command
- fix bugs with github api
- small changes to zupgr
- improved speed of ZPM programs
- fix bug with --full options in zupd program
- updated dependencies in INSTALLs. and INETINSTALL.sh
- updated zuninstall program

ZPM v2.0.2
- fix readme
- update zupgr program
- small fixes

ZPM v2.0.1
- small hotfix

ZPM v2.0
Program updates: (General)
- zhelp new UI
- new ZPM update system
- improved INSTALL.sh
- improved INETINSTALL.sh
- clean rebuild
- zupd rebuild
- new progressbar

ZPM v1.9
Program updates: (General)
- zhelp new UI, 
- extended arm support
- fix INETINSTALL.sh
- fix INSTALL.sh


ZPM v1.8
Program updates: (General)
- zhelp new UI, 
- add arm support
- fix INETINSTALL.sh

ZPM v1.7
Program updates: (General)
- zhelp new UI, 
- fix progressbar 
- added update info to zielina.conf
- now programs can detect new versions of ZPM and show you info
- added flatpak and snap support to zinfo, zlist and zsearch

ZPM v1.6
Added:
- PREVERSION.txt

Program updates: (General)
- zhelp new UI, 
- new --help look in all programs
- bug fixes
removed:
- changelog.md

ZPM v1.5
Added:
ZPM/zpm program, a new way of using Zielina_Package_Manager

Program updates:
INETINSTALL.sh
- updated logic, and fixed bugs
zuninstall: 
- updated logic
zupgr:
- fixed bugs
zinst:  no changes
zrm: no changes
zs: no changes
zr: no changes
zhelp: no changes
zclean: mo changes
zupd: no changes
zinfo: no changes
zlist: no changes
zsearch: no changes
-----------------------
Next update plans (v1.6):
 - fix bugs and improve programs functions


ZPM v1.4
Added:
- INETINSTALL.sh, now it it way easier to install ZMP from one command

Programs updates:
zupgr:
- fixed bugs
zuninstall: 
- updated logic, fixed bugs
zinst:  no changes
zrm: no changes
zs: no changes
zr: no changes
zhelp: no changes
zclean: mo changes
zupd: no changes
zinfo: no changes
zlist: no changes
zsearch: no changes
-----------------
Next update plans (v1.5)
- fix bugs and improve programs functions


ZPM v1.3 (Stable)

Programs updates:

zupd:

- Fixed options logic (--help, -h, --version, -v)

zinst:  no changes
zrm: no changes
zs: no changes
zr: no changes
zhelp: no changes

zsearch:
- Fixed options logic (--help, -h, --version, -v)

zinfo:
- Fixed options logic (--help, -h, --version, -v)

zupgr:
- Fixed options logic (--help, -h, --version, -v)

zuninstall: no changes
zlist:
- Fixed options logic (--help, -h, --version, -v)

zclean:
- Fixed options logic (--help, -h, --version, -v)


-----
Next update plans (v1.4):
- Find bugs, and improve programs functions



ZPM v1.2 (Stable)

Added:
- CHANGELOG.txt and CHANGELOG.md

Programs updates:

zupd:
- Added --yes (-y)
- Fixed --full (update)
- Fixed options logic (--help, -h, --version, -v)

zinst:
- Fixed options logic (--help, -h, --version, -v)

zrm:
- Fixed options logic (--help, -h, --version, -v)

zs:
- Added --yes (-y)
- Fixed options logic (--help, -h, --version, -v)

zr:
- Added --yes (-y)
- Fixed options logic (--help, -h, --version, -v)

zhelp:
- Updated documentation (added more info)

zsearch: no changes
zinfo: no changes
zupgr: no changes
zuninstall: no changes
zlist: no changes
zclean: no changes

---

Next update plans (v1.3):
- Improve logic in: zsearch, zinfo, zupgr, zuninstall, zlist, zclean
